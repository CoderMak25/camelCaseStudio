// mailer.js — currently unused. Email is handled on the frontend via EmailJS.
// We keep this file only to satisfy existing imports; functions are no-ops.

const sendContactEmail = async () => { };
const isEmailReady = () => false;

module.exports = { sendContactEmail, isEmailReady };
